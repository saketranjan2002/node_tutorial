//Modules
// node uses CommonJS
//CommonJS , every file is module (by default)
//Modules -Encapsulation code (only share minimmum)


const sayHi=(name)=>{
console.log(`Hello there, ${name}`);
}

sayHi('susan')
sayHi(john)
sayHi(peter)
